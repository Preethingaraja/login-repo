import './globals.css';

export const metadata = {
  title: 'Neural Gen-AI Networks Login',
  description: 'Welcome to our AI universe',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
